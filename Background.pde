class Background {
  int bgWidth;
  int bgHeight;

  Background(int width, int height) {
    bgWidth = width;
    bgHeight = height;
  }

  void display() {
    // Background display logic (currently empty)
  }
}
